var radius = prompt("Enter radius of circle :")
var PI = 3.14


var diameter = Number(radius) * 2;
var environment = Number(radius)* 2 * PI;
var area = Number(radius)* Number(radius) * PI;

alert("diameter :"+ diameter)
alert("environment :"+environment)
alert("area :" +area)